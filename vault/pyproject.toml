[project]
name = "quench-vault"
version = "0.1.0"
description = "Artifact store for sealed .ingot files"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
